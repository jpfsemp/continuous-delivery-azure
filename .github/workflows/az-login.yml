name: Az Login

on: workflow_dispatch

jobs:
  Login:
    runs-on: ubuntu-latest
    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          
       - run: az account show
